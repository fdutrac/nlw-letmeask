import React from 'react';

// import { Container } from './styles';
type ButtonProps {
    children?: string;
}

export const Button = (props: ButtonProps) => {
    return (
        <button>
            Botão!
        </button>
    )
}
